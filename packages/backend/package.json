{
  "name": "@param-logger/backend",
  "version": "0.1.3",
  "description": "Backend plugin for Param Logger",
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@param-logger/shared": "workspace:*"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@caido/sdk-backend": "^0.56.2",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.0.0",
    "vitest": "^2.1.9"
  }
}